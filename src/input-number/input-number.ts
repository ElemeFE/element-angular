import { Component, ChangeDetectionStrategy} from '@angular/core'

@Component({
  selector: 'el-input-number',
  changeDetection: ChangeDetectionStrategy.OnPush,
  template: `
  `,
})
export class ElInputNumber {
  
  constructor(
  ) {
  }
  
}
